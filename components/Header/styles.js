import css from 'styled-jsx/css';

export default css`
  .navbar {
    padding: 1.2rem 0;
    box-shadow: none;
    z-index: auto;
  }
  .logo {
    min-height: 48px;
  }
  .github {
    height: 32px;
  }
`;
