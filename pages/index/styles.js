import css from 'styled-jsx/css';
import { colors } from '/utils/variables';

export default css`
  .menu-label {
    color: ${colors.dark};
  }

  .card-wrapper {
    margin-top: 20px;
  }

  .map-container {
    margin: 0;
    padding: 0;
  }
`;
